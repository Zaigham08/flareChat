import 'package:flutter/material.dart';

const txtColor = Colors.black;
const whiteColor = Colors.white;
const bgColor = Colors.white;
const blackColor = Colors.black;
var primaryColor = Colors.green.shade400;
const lessDimColor = Color(0xFF878787);
const unSelectedItemColor = Color(0xFF606060);
const iconColor = Color(0xFF414141);
var dividerColor = const Color(0xFF3D3D3D).withValues(alpha: .6);
var textFieldColor = Colors.grey.shade300;
const pinFieldColor = Color(0xFF141414);
const cardColor = Color(0xFF1B1B1B);
const dimColor = Color(0xFF1B1B1B);
const blueColor = Color(0xFF007AFF);
const linkColor = Color(0xFF053962);
const btnColor = Colors.green;
const toastColor = Color(0xFF7C0606);
const kDefaultPadding = 18.0;

const internetExceptionString =
    "We're unable to show results.\nPlease check your Internet\nconnection";
const generalExceptionString =
    "We're unable to process your request.\nPlease try again";
const logoutString =
    "Are you sure you want to log out? You’ll need to log in again to access your account.";

